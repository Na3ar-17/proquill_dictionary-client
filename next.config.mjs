/** @type {import('next').NextConfig} */
const nextConfig = {
	swcMinify: true,
	fastRefresh: true,
	productionBrowserSourceMaps: false,
	optimizeFonts: false,
	minify: false,
}

export default nextConfig
